import { TreeSelect } from 'antd';
import React, { FC } from 'react';
import './index.less';

interface IProps {}

const Temp: FC<IProps> = () => {
  
  return <TreeSelect className="edit-ag-grid-tree-select"/>;
};

export default Temp;